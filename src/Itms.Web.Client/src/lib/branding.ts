/**
 * The names this product renders. Spelled once, because the organisation's name and
 * the system's name appear in the sidebar, on the login page, and in the browser tab,
 * and three copies of a name is three places to get it wrong.
 */

/** The organisation the system is operated by. Long by nature — layouts must wrap it. */
export const organisationName = 'Commonwealth Utilities Corporation'

/** The system itself. */
export const productName = 'ITMS'

/** The line under the organisation name. Rendered uppercase by the type token. */
export const productDescriptor = 'Unified IT Management'
