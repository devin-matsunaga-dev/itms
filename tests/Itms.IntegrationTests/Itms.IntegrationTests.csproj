<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- xUnit v3 test projects are self-executing. -->
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="Aspire.Hosting.Testing" />
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
    <Using Include="Shouldly" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Itms.Platform\Itms.Platform.csproj" />
    <ProjectReference Include="..\..\src\Itms.Contracts\Itms.Contracts.csproj" />
    <ProjectReference Include="..\..\src\Itms.Messaging\Itms.Messaging.csproj" />
    <ProjectReference Include="..\Itms.TestSupport\Itms.TestSupport.csproj" />
    <!-- WP-0.5: the authentication criteria are about what the wire carries, so the
         suite boots the real composition root rather than a stand-in. -->
    <ProjectReference Include="..\..\src\Itms.Web.Host\Itms.Web.Host.csproj" />
    <ProjectReference Include="..\..\src\Modules\Itms.Modules.Identity\Itms.Modules.Identity.csproj" />
    <!-- WP-0.6: the tree, the subtree rewrite, and the two lookup contracts are all
         statements about what the database and the wire actually do. -->
    <ProjectReference Include="..\..\src\Modules\Itms.Modules.Directory\Itms.Modules.Directory.csproj" />
    <!-- WP-1.1: the reference data is asserted over the wire — the role boundary, the
         409s the unique indexes stand behind, and that the seed migration actually ran. -->
    <ProjectReference Include="..\..\src\Modules\Itms.Modules.Helpdesk\Itms.Modules.Helpdesk.csproj" />
    <!-- WP-2.1: the asset tag's uniqueness and the per-manufacturer serial rule are both
         partial unique indexes; the 409 they stand behind is only demonstrable against a
         real PostgreSQL. -->
    <ProjectReference Include="..\..\src\Modules\Itms.Modules.Assets\Itms.Modules.Assets.csproj" />
    <!-- WP-3.1: invariant 6 is a statement about two modules — a device may only be
         registered over an asset another module owns — so it can only be shown over the
         wire, against the real IAssetLookup and the real unique index. -->
    <ProjectReference Include="..\..\src\Modules\Itms.Modules.Monitoring\Itms.Modules.Monitoring.csproj" />
    <!-- WP-0.7: the audit spine is asserted against the real table — the trigger that
         refuses an UPDATE cannot be demonstrated any other way. -->
    <ProjectReference Include="..\..\src\Modules\Itms.Modules.Audit\Itms.Modules.Audit.csproj" />
    <!-- The AppHost is referenced so its application model can be built and
         inspected. IsAspireProjectResource="false" keeps it an ordinary reference
         rather than something Aspire tries to orchestrate. -->
    <ProjectReference Include="..\..\src\Itms.AppHost\Itms.AppHost.csproj" IsAspireProjectResource="false" />
  </ItemGroup>

</Project>
